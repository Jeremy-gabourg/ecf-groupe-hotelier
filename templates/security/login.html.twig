{% extends 'base_front.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    <h1 class="h3 mt-4 mb-3 font-weight-normal text-center" class="form-label">Connexion</h1>

<div class="container-fluid">
{#    <form method="post">#}
        {{ form_start(loginForm) }}
        {% if authenticationError %}
            <div class="alert alert-danger">{{ authenticationError }}</div>
        {% endif %}
    <div>
        {{ form_row(loginForm.email) }}
    </div>
    <div>
        {{ form_row(loginForm.password) }}
    </div>
{#        {% if app.user %}#}
{#            <div class="mb-3">#}
{#                Vous êtes déjà connecté en tant que {{ app.user.username }}, <a href="{{ path('app_logout') }}">Déconnexion</a>#}
{#            </div>#}
{#        {% endif %}#}
{#        <label for="inputEmail">Email</label>#}
{#        <input type="email" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>#}
{#        <label for="inputPassword" class="form-label mt-4">Mot de passe</label>#}
{#        <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>#}

        <input type="hidden" name="_csrf_token"
               value="{{ csrf_token('authenticate') }}"
        >

        {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
        #}

        <div class="d-grid mx-auto col col-md-6 col-lg-4 mt-5">
            <button class="btn btn-lg btn-primary mt-3" type="submit">
                Se connecter
            </button>
        </div>
        {{ form_end(loginForm) }}
        <div class="text-center pt-3">
            <a href="/register" >
                Vous n'avez pas encore de compte? Cliquez sur ce lien pour en créer un!
            </a>
        </div>
    </form>

</div>
{% endblock %}
