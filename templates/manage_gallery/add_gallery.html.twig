{% extends 'base_back.html.twig' %}

{% block title %}Hello ManageGalleryController!{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('addGallery') }}
{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello ! ✅</h1>

    <div>
        {{ form_start(addGalleryForm) }}
        <div>
            {{ form_errors(addGalleryForm) }}
        </div>

        <div class="row">
            <div class="col">
                {{ form_row(addGalleryForm.galleryName) }}
            </div>
            <div class="col">
                {{ form_row(addGalleryForm.establishment) }}
            </div>
            <div class="col">
                {{ form_row(addGalleryForm.suite) }}
            </div>
            <div class="col">
                {{ form_row(addGalleryForm.highlightedPhoto) }}
            </div>
            <div class="col">
                {{ form_row(addGalleryForm.photos) }}
            </div>
        </div>
        {{ form_row(addGalleryForm.submit)}}
        {{ form_end(addGalleryForm) }}
    </div>
</div>
{% endblock %}
